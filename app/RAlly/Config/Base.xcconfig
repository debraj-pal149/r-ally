// Shared build settings. Secrets are optional so a clean clone still compiles.
#include? "Secrets.generated.xcconfig"

SWIFT_VERSION = 6.0
IPHONEOS_DEPLOYMENT_TARGET = 17.0
TARGETED_DEVICE_FAMILY = 1
CURRENT_PROJECT_VERSION = 1
MARKETING_VERSION = 1.0
